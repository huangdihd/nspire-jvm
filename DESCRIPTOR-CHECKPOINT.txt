Historical checkpoint: commit 7262cda (FileDescriptor / console / priority).
Counts and stopping points below describe that commit; see CHECKPOINT.md for
the current state and XINBOT-RUN.txt for the latest actual application attempt.

FileDescriptor / console / priority checkpoint

Implemented:
- Preserved OpenJDK FileDescriptor, FileInputStream, FileOutputStream,
  InputStream, SyncFailedException and JavaIOFileDescriptorAccess.
- Platform descriptor read/write/open/close/available and host fsync; original
  Java shared-close callbacks and suppressed exceptions execute normally.
- System.in/out/err use actual shared Java descriptors. Java closes invalidate
  aliases, retaining the embedding process/SDK diagnostic and UI handles.
- Descriptor ownership, GC roots and once-only cleanup on collection/VM exit.
- Real InputStream default methods; explicit native resource/byte-array overrides.
- Thread priority inheritance, range validation and weighted cooperative
  scheduling. Explicit yield/blocking still relinquish the current quantum.

Observed validation:
- The earlier FileStreamTest markSupported failure is fixed.
- The runtime builds with pinned Java 8 javac; 378 original source files.
- 48 core checks, including Java priority behavior and a separate VM scheduling
  policy test, pass on ordinary and ASan/UBSan/leak-detection builds.
- The complete existing runtime, loader, time, lambda, case, stream, annotation,
  regex, output, reflection, charset, file, SAX and Logback component suites pass
  on both builds. Detailed checks are recorded in their respective RESULTS files.
- Eleven file checks pass, including shared descriptors, console bytes, pipe
  input and GC/VM lifecycle. The embedding harness includes six Java console-close
  runs among six normal/fatal runs; later VM invocations retain working stdio.
- The unchanged Xinbot release reaches System.mapLibraryName in JansiLoader on
  both builds with no sanitizer error. See XINBOT-RUN.txt for the actual stack.
- ARM ELF links and the Zehn .tns is rebuilt; TARGET-RESULTS.txt records the
  header, symbol and size inspection. This is not a device execution test.

Reproduce descriptor checks in Linux/WSL:
  python3 tools/build-runtime.py --java8-home /path/to/java8-jdk
  make host build/file-lifetime build/nspire-jvm-asan build/file-lifetime-asan
  python3 tools/test-files.py --vm build/nspire-jvm --java /path/to/java8/bin/java --lifetime build/file-lifetime
  ASAN_OPTIONS=detect_leaks=1 UBSAN_OPTIONS=halt_on_error=1 python3 tools/test-files.py --vm build/nspire-jvm-asan --java /path/to/java8/bin/java --lifetime build/file-lifetime-asan
  python3 tools/test.py --vm build/nspire-jvm

dist/nspire-jvm.tns and dist/runtime.jar.tns have both been rebuilt for this
checkpoint. DIST-MANIFEST.json records the matching artifact sizes and hashes.
Preserved upstream source/notice hashes are recorded in SOURCES.json manifests.

Remaining boundaries:
  Xinbot.main has not been reached. Jansi native loading/platform calls, further
  JDK paths, network transport and calculator execution remain incomplete.
  Ndless line-buffered input and SDK storage visibility require device testing.
  Ndless sync throws SyncFailedException rather than claiming durable writes.
  FileChannel, generic JNI/finalization, ThreadGroup and full nonseekable-device
  semantics remain unsupported. Native blocking I/O blocks the interpreter;
  priority scheduling controls bytecode execution, not OS native thread priority.
