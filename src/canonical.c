/* Compile the unmodified upstream algorithm; its license is in vendor/. */
#ifndef _XOPEN_SOURCE
#define _XOPEN_SOURCE 700
#endif
#include "canonical.h"
#include "../vendor/openjdk8-file/canonicalize_md.c"
